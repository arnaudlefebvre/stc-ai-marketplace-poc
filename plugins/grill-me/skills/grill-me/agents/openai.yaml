interface:
  display_name: "Grill Me"
  short_description: "Stress-test a plan through focused questioning"
  default_prompt: "Use $grill-me to challenge this plan one question at a time and recommend an answer for each decision."
policy:
  allow_implicit_invocation: true
